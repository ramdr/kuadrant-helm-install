{{- if eq .Values.authorino.enabled false }}
{{ $args := dict "namespace" .Values.kuadrant.namespace "subscription" "kuadrant-operator" }}
{{ include "kuadrant-operators.olm-wait" $args }}
{{- end }}
